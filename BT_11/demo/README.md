# Demo Spring Boot Application

This project is a small Spring Boot demo migrated to Java 21.

Quick commands:

```powershell
mvn -v
mvn clean package
```

Notes:
- Project was upgraded to Java 21 and verified to build and pass unit tests.
